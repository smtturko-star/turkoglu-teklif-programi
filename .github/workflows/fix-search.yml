name: Fix search bars and clean stray text

# Disabled: this workflow used to rewrite index.html automatically and could
# corrupt the working application. Search UI changes must be made deliberately.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled to protect the production index.html"
