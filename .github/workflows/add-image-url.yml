name: Add product image URL support

# Disabled: product image support must not rewrite index.html automatically.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled to protect the production index.html"
