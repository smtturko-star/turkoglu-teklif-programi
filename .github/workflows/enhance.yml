name: Enhance application UI

# Disabled: this workflow rewrote index.html automatically and could override
# the known-good application state.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled to protect the production index.html"
