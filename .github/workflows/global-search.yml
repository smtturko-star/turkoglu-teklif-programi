name: Install stable search bars

# Disabled: automatic index.html rewriting is intentionally stopped.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled to protect the production index.html"
