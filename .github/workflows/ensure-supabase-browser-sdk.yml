name: Ensure Supabase browser SDK

# Disabled: index.html is no longer patched automatically by Actions.
on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled to protect the production index.html"
